    <!-- Contact Section -->
    <section>
        <% data.portfolio.each do |portfolio| %>
        <div class="portfolio-section" style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.2)), url('<%= portfolio["photo-portfolio-section"] %>');">
            <% data.contact.each do |contact| %>
            <%= contact["name"] %>
            <%= contact["mail"] %>
            <% end %>
        </div>
    </div>
    <% end %>
</section>
